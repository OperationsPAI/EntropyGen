# Secret templates for platform components.
# Most secrets are created via scripts/init-secrets.sh
# This template handles non-sensitive default configuration.
# Filled in: Phase 2+ as components are implemented
